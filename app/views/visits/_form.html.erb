<%= form_for([@visit.patient, @visit]) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field d-flex justify-content-center ">
  <div class="form-group">
    <%= f.label :doctor_comments %><br />
    <%= f.text_field :doctor_comments , class: "form-control", style: "height: 200px ; width: 500px" %>
   </div>
  </div>
 
  <div class="actions d-flex justify-content-center">
    <%= f.submit  class: "btn btn-primary my-3 " , value: "Add Comment for Patient" %>
  </div>
  
  </div>
<% end %>
